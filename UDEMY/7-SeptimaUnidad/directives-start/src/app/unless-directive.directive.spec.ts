import { UnlessDirectiveDirective } from './unless-directive.directive';

describe('UnlessDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new UnlessDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
